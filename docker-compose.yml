version: "3.8"

services:
  postgres:
    image: "postgres:latest"
    container_name: epsilon-db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: epsilon_db
    ports:
      - "5432:5432"
